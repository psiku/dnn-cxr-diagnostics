data_processing:
  pathology_list:
    - "Infiltration"
    - "Effusion"
    - "Atelectasis"
    - "Nodule"
    - "Mass"
    - "Pneumothorax"
    - "Consolidation"
    - "Pleural_Thickening"
    - "Cardiomegaly"
    - "Emphysema"
    - "Edema"
    - "Fibrosis"
    - "Pneumonia"
    - "Hernia"

  columns_to_keep:
    - "image_index"
    - "finding_labels"
    # - "patient_age"
    # - "patient_sex"
    # - "view_position"

  ohe_column: "finding_labels"

data_processing_precompute:
  images_dir: data/01_raw/images
  out_dir_full: data/05_model_input/tensor_full_${globals:resize}
  out_dir_crop: data/05_model_input/tensor_cropped_${globals:resize}
  out_dir_mask: data/05_model_input/masks_cropped_${globals:resize}
  image_size: ${globals:resize}
  compute_tensors: true
  image_type: "full" # Options: "full", "cropped", "cropped_with_mask"